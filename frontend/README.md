This is a minimal React frontend skeleton for the TicketFlow project.

To run locally (requires Node.js):

```bash
cd frontend
npm install
npm start
```

To build into static files served by Flask:

```bash
npm run build
# copy build output to ticket_system/ticket_system/static or configure Flask to serve it
```
